name: Page Tests
on: [push, pull_request]

jobs:
  test:
      runs-on: ubuntu-latest
      steps:
        - uses: actions/checkout@v4
        - name: Validate HTML
          uses: setup-node@v4
          with: node-version: 20
          - run: npm install
          - run: npm test
